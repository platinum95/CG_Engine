OpenGL core functionality, calls, etc handled by code in CG_Engine folder.

Code in this folder is "implementation" of the "Engine"

This repo is a 'fresh' remake of what is now CG_Engine_old, which became excessively large due to old commits containing many assets.